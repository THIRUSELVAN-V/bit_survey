
import {BrowserRouter as Router } from 'react-router-dom';
import { PublicRoute } from './routes';

const App = () => {
  
  return (
    <Router>
        <PublicRoute/>
    </Router>
  );
};

export default App;
